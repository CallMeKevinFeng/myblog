{
  "buildCommand": "hugo --gc --minify",
  "outputDirectory": "public",
  "installCommand": "if command -v hugo &> /dev/null; then echo 'Hugo found'; else curl -L -o hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.140.0/hugo_extended_0.140.0_linux-amd64.deb && dpkg -i hugo.deb && rm hugo.deb; fi",
  "framework": "hugo"
}
